<%= simple_form_for(:scene) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= #f.association :location, collection: Location.all(order: 'name') 
    %>
  </div>
  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
