<h1><%= link_to 'Characters', universe_characters_path(@universe) %></h1>

<table>
  <tr>
    <th>Name</th>
  </tr>

<% @universe.characters.each do |character| %>
  <tr>
    <td><%=  link_to character.name, universe_characters_path(@universe) %></td>
  </tr>
<% end %>
</table>


