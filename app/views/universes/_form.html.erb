<%= simple_form_for(@universe) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :users_id %>
    <%= f.association :users %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
