<h1>Scenes in the <%= link_to @scene.name, storyarc_scenes_path(@scene.storyarc.id), :method => :get %></h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
  </tr>
<% @branches.each do |branch| %>
  <tr>
	<td><%= best_in_place [branch], :name, :type => :input %></td>

  </tr>
<% end %>
</table>
<br />
<%= render 'form' , :locals =>  {:branch => @branch} %>



