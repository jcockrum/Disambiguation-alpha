
	<h1><%= link_to 'Stories', storyarc_stories_url %></h1>

	<table>
	    <tr>
	        <th>Name</th>
	    </tr>
	    <%  @stories.each do |story| %>
	    <tr><td><%= story.name %></td></tr>
	    <% end %>
	</table>
