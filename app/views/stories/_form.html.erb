<%= simple_form_for([@universe, @story]) do |f| %>
  <%= f.error_notification %>
  <%= f.input :name %>
  <%= f.input :chapter %>
  <%= f.association :scenes, as: :check_boxes %>
  <%= f.error :base %>
  <%= f.button :submit %>
<% end %>

